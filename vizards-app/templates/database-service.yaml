{{- if .Values.database.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: vizards-database
spec:
  selector:
    app: database
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
{{- end }}